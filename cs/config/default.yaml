dataset:
  name: freyfaces
  batch_size: 10
  test_prop: 0.5

loss:
  name: elbo_unconstrained
  lr: 0.0001
  epochs: 250
  ksi: 0.0001

model:
  seed: 0
  h: 200
  d: 5

defaults:
  - _self_

hydra:
  job:
    chdir: true
  run:
    dir: ./outputs/${loss.name}_${now:%Y-%m-%d}/${now:%H-%M-%S}